# Known Issues

- **Memory Usage:** The Hash Map approach utilizes $O(n)$ space, which may be a concern for extremely memory-constrained environments.

- **Single Solution Limitation:** The current implementation returns only the first valid pair found; it does not list all possible pairs if multiple exist.